# Template .env example for Minio — safe to commit
# Replace placeholders with your values in your local .env
MINIO_ENDPOINT=your-minio-host:9000
MINIO_ACCESS_KEY=YOUR_ACCESS_KEY
MINIO_SECRET_KEY=YOUR_SECRET_KEY
MINIO_USE_SSL=true
# Optional: default bucket name
MINIO_DEFAULT_BUCKET=your-default-bucket
